# Copy to `.env` and fill in with the endpoints of the stack you want to hit
# when invoking the CLI with `--env local`. This file is read at runtime
# (from process.cwd()) and never bundled into the packaged CLI.

VIBECO_API_BASE_URL=
VIBECO_OAUTH_ISSUER_URL=
VIBECO_OAUTH_CLIENT_ID=
